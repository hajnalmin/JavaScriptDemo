/**
 * 1.  写一个方法能准确获取数据类型 ，typeof 获取数据类型时并不准确。typeof 数组时得到的也是object
 * 2.  写一个方法将数据转换成整数
 * 3.  写一个方法将数据转换成数字，可保留小数
 * 4.  写一个方法判断数组中是否包含某个数据，原生写要兼容IE8及以下
 * 5.  写一个方法判断对象中是否包含某个数据
 * 6.  写一个方法对数据进行合并，无论何种数据类型都要能进行合并
 * 7.  写一个方法统计对象中有多少个属性
 * 8.  写一个兼容方法能获取浏览器可视区域大小，兼容所有浏览器！！！
 * 9.  写一个兼容方法清除字符串两边的空格，兼容IE8及以下
 * 10. 写一个方法可以遍历任何数据类型！
 * 11. 写一个方法可以获取数组中指定位置的元素，比如 3,6 获取索引值第3~第6的元素
 * 12. 写一个兼容方法可以通过class查找元素，兼容IE8及以下
 * 13. 写一个方法 根据  ul li 这个选择器在页面中查找元素
 * 14. 写一个方法判断元素是否有某个类名
 * 15. 写一个方法可以点击拖动元素，鼠标按着拖动，鼠标松开则不能继续拖动。
 * 16. 不使用数组原生方法 自己写 push  shift unshift pop slice splice concat 等方法
 * 
 */